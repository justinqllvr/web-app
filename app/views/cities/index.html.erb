<h1 class="title_cities">Villes</h1>



<table class="table container table-hover">
  <thead class="thead-dark">
    <tr>
      <th>Villes</th>
      <th>Signalements</th>
    </tr>
  </thead>
  <tbody>
    <% @cities.each do |city| %>
      <tr>
        <td><%= link_to city.name, city %></td>
        <td><%= city.reports.count %></td>
      </tr>
    <% end %>
  </tbody>
</table>