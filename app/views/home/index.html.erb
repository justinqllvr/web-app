<div id="map"></div>
<script src="https://maps.googleapis.com/maps/api/js?key=<%= ENV['GOOGLE_MAP_API_KEY'] %>&callback=initMap"async defer></script> <!--Google Map API-->
<script>
function initMap() {
    var map = new google.maps.Map(document.getElementById('map'), {
        zoom: 13
    });  
    <% @reports.each do |report| %>
        var marker = new google.maps.Marker({
        position: new google.maps.LatLng(<%= report.latitude %>, <%= report.longitude %>),
          url: '<%= report_path(report) %>'
        });
        marker.setMap(map);

    
    google.maps.event.addListener(marker, 'click', function() {
        window.location.href = this.url;
    });

    <% end %>
    
     if (navigator.geolocation) {
          navigator.geolocation.getCurrentPosition(function(position) {
            var pos = {
              lat: position.coords.latitude,
              lng: position.coords.longitude
            };

            map.setCenter(pos);
          });

        } 

        else {
          // Browser doesn't support Geolocation
          var pos = {
              lat: 44.841713,
              lng: -0.569101
            };
          map.setCenter(pos);
        }
      

    
}
</script>

<%= link_to "Faire un signalement", new_report_path, class: "btn btn-info new-report" %>

<div class="container mt-5" style="text-align: center;">
  <h2> Nos partenaires </h2>
  <div class="row partenaires">
    <div class="col-md-4 d-flex">
      <%= image_tag 'https://files.slack.com/files-pri/TBU3TSYVA-FGZQ53H0T/mmilogo.png' %>
    </div>
    <div class="col-md-4 d-flex">
      <%= image_tag 'http://ausoniuseditions.u-bordeaux-montaigne.fr/images/Images/Bordeaux-Montaigne-Logo2.png' %>
    </div>
    <div class="col-md-4 d-flex">
      <%= image_tag 'https://www.tousbenevoles.org/images/association/1445517729.jpg' %>
    </div>
  </div>
</div>