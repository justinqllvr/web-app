<div class="container g-content">
	<h1>Poste ton problème</h1>

	<%= render 'form', report: @report %>

	<div class="row justify-content-center" style="padding-top: 5px;">
	    <% pre_like = @report.likes.find { |like| like.user_id == current_user.id} %></div>

</div>